# Git Deploy Extractor PRD

> Version: 0.1
> Status: Draft
> 기준 문서: REQUIREDMENT.md, UI_UX_SPEC.md

---

# 1. 제품 개요

망분리 환경에서 외부망 → 내부망으로 배포 파일을 옮기는 과정이 전량 수작업이라, 파일 누락·잘못된 파일 포함·중복 작업이 반복적으로 발생한다(REQUIREDMENT.md §2). Git Deploy Extractor는 이 중 "어떤 파일을 배포해야 하는가"를 Git 이력에서 정확히 계산해주는 데스크톱 도구다. Git Push/Pull/Merge, GitLab CI, 운영 서버 배포는 다루지 않는다(§3) — 판단이 필요한 부분(무엇을 배포할지)만 자동화하고, 실제 반영은 여전히 사람이 한다.

---

# 2. 타겟 사용자

**외부망 개발자** — 외부망(인터넷 가능)에서 개발하고, 변경분을 내부망(인터넷 불가)으로 수동 전달해야 하는 사람. Git/커밋 단위 작업에는 익숙하지만, 어떤 파일이 실제로 바뀌었는지 diff를 손으로 추적하는 건 반복 작업이자 실수 유발 지점이다.

---

# 3. User Story

여정 단계별로 묶었다. 각 스토리는 REQUIREDMENT.md의 REQ-XXX 하나 이상에 대응한다.

## 3.1 선택 단계

- 외부망 개발자로서, 로컬 Git Repository를 선택하고 싶다. 프로젝트마다 매번 경로를 다시 찾지 않기 위해서다. (REQ-001)
- 외부망 개발자로서, 배포 기준 Branch를 선택하고 싶다. 어떤 브랜치 기준으로 배포 파일을 뽑을지 명확히 하기 위해서다. (REQ-002)
- 외부망 개발자로서, Commit 목록에서 Hash/Author/Date/Message를 보고 싶다. 어떤 작업이 언제 누구에 의해 이뤄졌는지 확인하고 배포 대상을 정확히 고르기 위해서다. (REQ-003)
- 외부망 개발자로서, 여러 Commit을 한 번에 선택하고 싶다. 배포 단위가 Commit 하나가 아니라 여러 작업의 묶음인 경우가 많기 때문이다. (REQ-004)

## 3.2 계산 단계 (자동)

- 외부망 개발자로서, 선택한 Commit들의 변경 파일이 Merge/Rebase/Cherry-pick 여부와 무관하게 정확히 계산되길 원한다. Git 전략에 따라 결과가 달라지면 신뢰할 수 없기 때문이다. (REQ-005, DR-005)
- 외부망 개발자로서, 같은 파일이 여러 Commit에서 바뀌었어도 배포 목록엔 한 번만 나오길 원한다. 중복 항목을 손으로 걸러내고 싶지 않기 때문이다. (REQ-006)
- 외부망 개발자로서, 실제로 복사되는 파일은 항상 브랜치 HEAD의 최신 버전이길 원한다. Commit 시점의 옛날 파일이 섞여 들어가면 안 되기 때문이다. (REQ-007)
- 외부망 개발자로서, Spring 표준 구조를 따르는 프로젝트라면 별도 설정 없이도 경로가 그대로 유지되길 원한다. 매번 매핑 규칙을 만드는 건 불필요한 작업이기 때문이다. (REQ-008)

## 3.3 확인/보정 단계

- 외부망 개발자로서, 자동 계산된 배포 대상 파일 중 실제로 필요 없는 파일을 개별적으로 빼고 싶다. 자동 계산이 항상 내 의도와 100% 일치하지는 않기 때문이다. (REQ-011)
- 외부망 개발자로서, 목록 전체를 한 번에 선택/해제하고 싶다. 파일이 많을 때 하나씩 체크하는 건 비효율적이기 때문이다. (REQ-011)

## 3.4 산출 단계

- 외부망 개발자로서, 배포 대상 파일만 별도 디렉터리로 모아서 받고 싶다. 원본 저장소를 그대로 옮기지 않고 필요한 것만 깔끔하게 전달하기 위해서다. (REQ-009)
- 외부망 개발자로서, 배포/삭제 대상 파일과 원본 커밋 이력을 정리한 텍스트(`extract-list.txt`)를 함께 받고 싶다. 내부망에 반영할 때 무엇을 지우고 무엇을 추가했는지 근거 자료로 남기기 위해서다. (REQ-010, 정정 2026-09-24 — 세 파일이 하나로 통합됨)

---

# 4. 기능 우선순위

전부 MVP 확정 범위(REQUIREDMENT.md §11)이므로 "자를 수 있는 기능"은 없다. 대신 **이게 없으면 도구가 아예 성립하지 않는 것(P0)**과 **정확성/안전을 위해 필요한 것(P1/P2)**으로 나눠 구현 순서의 근거로 삼는다.

| 우선순위 | REQ | 근거 |
|---|---|---|
| **P0 (Critical Path)** | REQ-001, 002, 003, 004, 005, 006, 007, 009 | 이 8개만으로도 "Commit 선택 → 정확한 파일 계산 → 패키지 생성"이라는 최소 가치 루프가 완성된다. 하나라도 빠지면 도구의 존재 이유가 없다 |
| **P1 (정확성 필수)** | REQ-008, REQ-010 | 없어도 P0 루프는 돌아가지만, Mapping이 필요한 프로젝트(REQ-008)나 내부망 반영 근거 자료(REQ-010)가 없으면 특정 상황에서 잘못된 결과물이 나가거나 추적이 불가능해진다 |
| **P2 (안전장치)** | REQ-011 | 자동 계산 결과에 대한 최종 확인/보정 수단. 개발 중 UI/UX 명세 작업 과정에서 뒤늦게 필요성이 확인되어 추가된 안전판 성격의 기능이다 |

구현 순서는 P0 → P1 → P2를 권장하되, REQ-011(개별/전체 선택)은 DeployFilesPanel이라는 P0 화면 요소 위에 얹는 기능이라 별도 스프린트로 분리해도 무방하다.

---

# 5. Acceptance Criteria

| REQ | Given | When | Then |
|---|---|---|---|
| REQ-001 | 유효한 Git Repository 경로가 주어짐 | 사용자가 [Browse...]로 해당 경로를 선택 | Repository가 유효한 것으로 표시되고 Branch 목록이 로드된다 |
| | | 유효하지 않은 경로(Git repo 아님)를 선택 | "Git 저장소가 아닙니다" 등 구체적 에러가 표시되고 이후 단계로 진행되지 않는다 |
| REQ-002 | Repository가 유효하게 선택된 상태 | 사용자가 Branch dropdown에서 브랜치를 변경 | Commit List가 초기화되고 새 브랜치의 첫 페이지 Commit이 로드된다 |
| | Repository 선택 완료, Branch 목록 로드됨 | (자동) | `main`이 있으면 `main`, 없으면 `master`가 자동 선택된다. 둘 다 없으면 자동 선택하지 않는다 |
| REQ-003 | 브랜치가 선택된 상태 | Commit List가 로드됨 | 각 행에 Hash/Author/Date/Message가 표시된다 |
| | 조회 기간/최대 개수를 변경하지 않은 상태 | Commit List가 로드됨 | 기본값(오늘-7일~오늘, 최대 100개) 범위 내 Commit만 조회된다 — Branch 전체 이력을 한 번에 조회하지 않는다 |
| | | 사용자가 Search에 검색어 입력 | 해당 검색어가 포함된 Commit만 필터링되어 표시된다 |
| | | 사용자가 목록 하단까지 스크롤 | 누적 로드 개수가 `maxCount` 미만이면 다음 페이지가 로드되고, `maxCount`에 도달하면 더 이상 요청하지 않는다 |
| | | 사용자가 조회 기간 또는 최대 개수를 변경 | Commit List가 초기화되고 새 조건으로 재조회된다 |
| REQ-004 | Commit List가 로드된 상태 | 사용자가 여러 행의 체크박스를 선택 | 선택된 모든 Commit이 이후 계산의 입력으로 사용된다 |
| REQ-005 | 선택된 Commit 중 Merge Commit이 포함됨 | 변경 파일 계산이 실행됨 | Merge/Squash/Rebase/Cherry-pick 여부와 무관하게 각 Commit의 변경 파일 집합이 정확히 계산된다(DR-005) |
| REQ-006 | 동일 파일이 선택된 여러 Commit에서 각각 수정됨 | 변경 파일 목록이 계산됨 | 최종 Deploy Files 목록에 해당 파일이 1회만 나타난다 |
| REQ-007 | 선택 Commit 시점 이후 같은 파일이 추가로 수정된 이력이 있음 | Deploy Files가 계산됨 | 복사되는 파일 내용은 Commit 시점이 아니라 기준 Branch HEAD의 최신 버전이다(DR-003) |
| REQ-008 | Spring 표준 구조(src/main/java, src/main/resources) 프로젝트 | Mapping Rule이 적용됨 | Local Path와 Server Path가 동일하게 유지된다(별도 설정 불필요) |
| | Mapping Profile에 특정 경로 override가 정의된 경우 | 해당 경로의 파일이 포함됨 | override에 정의된 target 경로로 변환되어 표시된다(DR-010) |
| REQ-009 | Deploy Files 목록이 확정된 상태 | 사용자가 [Export] 클릭 | `git-deploy-extracted/` 디렉터리가 생성되고 대상 파일이 원본 구조 그대로 복사된다 |
| REQ-010 | [Export] 실행 완료 | Export 결과 확인 | `extract-list.txt` 하나가 UTF-8/LF로 생성된다(정정, 2026-09-24 — 예전엔 3개 파일로 분리돼 있었다) |
| REQ-011 | "포함된 파일" 목록에 여러 파일이 표시됨(정정, 2026-09-24 — Preview 직후 기본값은 전체 미선택) | 사용자가 개별 파일 체크박스를 체크 | 해당 파일이 "Extract 대상"으로 옮겨져 `extract-list.txt`와 실제 복사 대상에 포함된다 |
| | 화면 검색/패턴으로 일부 파일만 표시된 상태 | 사용자가 "전체 선택" 토글 클릭 | **표시된 행만** 일괄 Extract 대상으로 이동하고, 숨겨진 행은 영향받지 않는다(UI_UX_SPEC.md §2.6) |

---

# 6. 성공 지표

기존 수작업 프로세스에 대한 사용 데이터(베이스라인)가 없으므로, 목표 수치를 임의로 못 박기보다 **무엇을, 어떻게 측정할지**를 먼저 정의한다. 구체 목표값은 초기 사용 데이터 확보 후 확정한다.

| 지표 | 측정 방법 | 연결되는 문제(REQUIREDMENT.md §2) |
|---|---|---|
| 배포 파일 누락/오류 발생률 | 내부망 반영 후 "파일이 빠졌다/잘못 들어갔다"로 인한 재작업·핫픽스 건수 | 파일 누락, 잘못된 파일 포함 |
| 배포 준비 소요 시간 | Repository 선택 시작 ~ Export 완료까지 걸린 시간 (수작업 대비 비교는 초기 몇 건은 수동 기록 필요) | 중복 작업 |
| 대용량 Repository 체감 성능 | 수천 개 Commit 규모 저장소에서 Commit List 초기 로드/스크롤 시 UI 응답 지연 | §10 비기능요구사항 |
| 실사용 채택률 | 수작업 대신 이 도구를 사용한 배포 건수 비율 | 도구 자체의 존재 이유 검증 |

---

# 7. 범위 밖

REQUIREDMENT.md §3, §11과 동일. Git Push/Pull/Merge, GitLab/GitHub CI 연동, FTP/SFTP 전송, 운영 서버 배포는 이 제품의 책임이 아니다. Deploy Package 생성까지만 책임진다(구현 원칙 #5).
