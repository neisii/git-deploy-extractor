# Git Deploy Extractor 요구사항 정의서 (MVP)

> Version: 0.1
> Status: Draft
> Author: TBD

---

# 1. 프로젝트 개요

## 1.1 목적

Git Commit 이력을 기반으로 배포 대상 파일을 자동 추출하여 망 분리 환경에서 안전하게 내부망으로 전달할 수 있는 배포 패키지를 생성하는 크로스플랫폼 Desktop Application을 개발한다.

본 도구는 Git Repository 간 동기화 도구가 아니며, 운영 서버 배포 도구도 아니다.

"배포해야 하는 파일을 정확하게 추출하는 것"이 핵심 목적이다.

---

# 2. 개발 배경

현재 프로젝트는 외부망과 내부망이 물리적으로 분리되어 있다.

## 외부망

- 인터넷 연결 가능
- 개발 수행
- 외부 Git Server 사용

## 내부망

- 인터넷 연결 불가
- 내부 Git Server 사용
- GitLab CI를 통한 배포 수행

외부망에서 개발한 내용을 내부망으로 전달하기 위해서는

- 변경 파일 추출
- 파일 복사
- 파일 전송
- 내부 Git 반영

과정을 모두 수작업으로 수행하고 있다.

이 과정에서

- 파일 누락
- 잘못된 파일 포함
- 중복 작업

등이 자주 발생한다.

---

# 3. 프로젝트 범위

본 프로젝트의 책임 범위

- Local Git Repository 분석
- Commit 기반 변경 파일 추출
- 최신 버전 파일 생성
- 배포 패키지 생성

본 프로젝트 범위 외

- Git Push
- Git Pull
- Git Merge
- GitLab CI
- 운영 서버 배포
- FTP/SFTP 전송

---

# 4. 배포 환경

## 전체 배포 흐름

```text
                외부망
──────────────────────────────────────────────

Local Git Repository

↓

contract2/main

↓

Git Deploy Extractor

↓

git-deploy-extracted/

↓

파일전송 프로그램

═══════════════════════════════════════

                내부망

↓

Local Git Repository

↓

git add

↓

git commit

↓

git push

↓

내부 Git Server

↓

GitLab CI

↓

운영 서버 배포
```

Git Deploy Extractor는 git-deploy-extracted 폴더 생성까지만 책임진다.

---

# 5. 용어 정의

| 용어 | 설명 |
|------|------|
| Repository | Local Git Repository |
| 기준 브랜치 | 배포 기준 브랜치 (예: contract2/main) |
| 선택 Commit | 사용자가 배포 대상으로 선택한 Commit |
| 배포 대상 파일 | 선택 Commit에서 변경된 파일의 최신 버전 |
| Mapping Rule | Local Path → Server Path 변환 규칙 |
| Deploy Package | 생성된 배포 폴더 |

---

# 6. 핵심 요구사항

## REQ-001 Repository 선택

사용자는 Local Git Repository를 선택할 수 있어야 한다.

---

## REQ-002 Branch 선택

배포 기준 Branch를 선택할 수 있어야 한다.

예)

contract2/main

Repository 선택 시 기본 Branch가 자동 선택된다.

기본 Branch는 main 또는 master 중 존재하는 Branch이다(main 우선).

둘 다 없으면 자동 선택하지 않고 사용자가 직접 선택해야 한다.

---

## REQ-003 Commit 조회

선택한 Branch의 Commit 목록을 조회할 수 있어야 한다.

표시 정보

- Commit Hash
- Author
- Date
- Message

Branch의 전체 이력을 한 번에 조회하지 않는다.

조회 기준은 Commit 시점의 시작일~종료일과 최대 조회 개수이며, 둘 다 사용자가 조정할 수 있다.

기본 시작일은 오늘로부터 7일 전, 기본 종료일은 오늘이며, 기본 최대 개수는 100개이다.

시작일~종료일 범위는 시작일 00:00:00부터 종료일 23:59:59까지를 포함한다.

---

## REQ-004 Commit 다중 선택

사용자는 여러 Commit을 동시에 선택할 수 있어야 한다.

---

## REQ-005 변경 파일 추출

선택된 Commit들의 변경 파일을 모두 수집한다.

Merge 여부와 관계없이 변경 파일을 식별한다.

---

## REQ-006 중복 제거

동일 파일이 여러 Commit에 포함되어 있더라도

최종 결과에는 1회만 포함한다.

---

## REQ-007 최신 파일 추출

배포 파일은 Commit 시점의 파일이 아니라

기준 Branch HEAD의 최신 파일을 사용한다.

---

## REQ-008 Mapping Rule 적용

Local Path를 Server Path로 변환한다.

본 문서의 예시는 전형적인 Spring Framework 프로젝트 구조를 기준으로 한다.

src/main/java, src/main/resources/{static, templates, ...}

이 구조에서는 Local Path와 Server Path가 동일하다.

`.java` 파일은 원본 패키지 경로를 그대로 유지한다. (DR-011)

resources 하위 파일도 원본 경로를 그대로 유지한다. (DR-012)

Mapping Rule은 이 기본 구조와 다른 배포 경로가 필요한 경우에만 적용한다. (DR-010)

예)

src/main/java/com/example/sell/interfaces/receipt/controller/GuaranteeListController.java

↓

git-deploy-extracted/src/main/java/com/example/sell/interfaces/receipt/controller/GuaranteeListController.java

src/main/resources/static/js/guarantee/list.js

↓

git-deploy-extracted/src/main/resources/static/js/guarantee/list.js

---

## REQ-009 Deploy Package 생성

배포 파일만 별도 디렉터리로 생성한다.

---

## REQ-010 Export

다음을 Export할 수 있어야 한다.

- deploy-files.txt
- delete-list.txt
- deploy-summary.json

---

## REQ-011 배포 파일 개별 선택

사용자는 자동 계산된 배포 대상 파일 중 개별 파일을 배포 대상에서 제외할 수 있어야 한다.

실제로 필요한 파일만 선택해서 추출할 수 있어야 하기 때문이다.

전체 파일을 한 번에 선택/해제하는 전체 선택 토글을 제공한다.

---

## REQ-012 Export 경로 선택

사용자는 Export 결과물(`git-deploy-extracted/`)이 생성될 **부모 디렉터리**를 선택할 수 있어야 한다.

디렉터리 선택은 OS 네이티브 폴더 선택 다이얼로그로만 이루어진다(자유 텍스트 경로 입력 없음).

선택하지 않으면 저장소 루트가 기본값이다.

하위 폴더 이름(`git-deploy-extracted`)은 사용자가 바꿀 수 없다 — RISK_ISSUES.md 결정 이력 #20("이름은 고정이어야 예측 가능한 표준 산출물")과 일관성을 유지한다. 이번 요구사항은 이름이 아니라 **위치**만 다룬다.

선택한 경로는 앱을 재실행해도 유지된다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.1, 2026-08-07)

---

## REQ-013 의존성 완결성 검사 (Java/Spring)

사용자는 배포 대상 파일 목록에 Java/Spring 의존성이 누락되지 않았는지 확인할 수 있어야 한다.

키워드로 커밋을 검색해 배포 대상을 고르는 방식은 관련 커밋을 놓칠 수 있다 — 실제로 다른 팀이 공통 구현체를 수정한 커밋이 검색어에 걸리지 않아 내부망 빌드가 실패한 사례가 있었다.

Java/Spring 단일 모듈(멀티모듈 미지원)을 대상으로, `@SpringBootApplication` 클래스의 패키지를 기준 삼아 우리 코드 범위를 정한다. 이 기준 패키지는 실제 애노테이션을 찾아서 판단하며, 특정 회사/프로젝트의 패키지명을 코드에 미리 넣어두지 않는다.

의존성은 import 문(텍스트 참조)과 Spring DI(생성자/필드로 주입되는 인터페이스 → 그 인터페이스를 구현하면서 `@Component`/`@Repository`/`@Service` 등 stereotype 애노테이션이 붙은 클래스) 두 경로 모두로 판정하며, 깊이 제한 없이 전이적으로 추적한다.

같은 인터페이스를 구현하는 클래스가 여러 개면 하나로 확정하려 하지 않고 전부 후보로 제시한다 — 최종 판단은 사용자가 한다.

찾아낸 후보는 배포 대상 파일 목록에 개별적으로 추가하거나 한 번에 모두 추가할 수 있어야 한다.

`@SpringBootApplication` 클래스를 찾지 못하면 이 기능은 비활성화된다(폴백 없음).

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.2, 2026-08-07)

---

## REQ-014 분할 영역 크기 조절

나뉜 화면 영역의 비율을 사용자가 마우스 드래그로 조절할 수 있어야 한다.

- 좌우 분할(폭 조절): 커밋 목록/분석 요약, 포함된 파일/누락된 의존성
- 상하 분할(높이 조절): 커밋 목록·분석 요약 영역 전체 / 포함된 파일·누락된 의존성 영역 전체

조절한 비율은 앱을 재실행해도 유지된다.

각 영역은 최소 크기 아래로는 줄어들지 않으며, 창이 두 영역의 최소 크기 합보다 좁아지거나(좌우) 짧아지면(상하) 스크롤이 나타난다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.4, 2026-08-07). 상하 분할은 §7.4 최초 구현 직후 추가 요청으로 확장(2026-08-07, 결정 이력 #27).

---

## REQ-015 커밋 선택 유지

사용자는 검색 조건(검색어, 검색 대상, 조회 기간, 최대 개수)을 바꿔가며 여러 번 재조회해도, 이전에 체크한 커밋 선택을 잃지 않아야 한다.

키워드 검색만으로는 관련 커밋을 놓칠 수 있어(예: 검색어를 바꿔가며 여러 번 찾아야 하는 경우), 검색할 때마다 선택이 초기화되면 매번 다시 체크해야 하는 불편이 있다.

단, 다음 두 경우는 선택을 지운다(DR-015).

- Repository를 전환할 때 — 다른 저장소의 커밋 hash가 남아있으면 오류·오동작으로 이어질 수 있다.
- Branch를 전환할 때 — 서로 다른 Branch의 커밋이 한 Export에 섞일 위험이 있다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §6.1, 2026-08-07)

---

## REQ-016 파일명으로 커밋 검색

사용자는 커밋 메시지가 아니라 **변경된 파일명**으로 커밋을 검색할 수 있어야 한다.

메시지 검색과는 별도 모드이며, 사용자가 "메시지"/"파일명" 중 하나를 선택해서 검색한다.

검색 대상은 현재 선택된 Branch의 **HEAD 트리에 존재하는 파일명**만이며, 파일 경로의 마지막 조각(파일명)에 대한 부분 일치로 판단한다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.3, 2026-08-07)

---

## REQ-017 GitHub Release 업데이트 알림

사용자는 앱을 켤 때마다(또는 수동으로) 지금 쓰는 버전보다 새 GitHub Release가 있는지 확인할 수 있어야 한다.

새 버전이 있으면 RepositoryPanel 우측의 현재 버전 표시(`vX.Y.Z`)가 강조 배경으로 바뀌고, hover 시 title 툴팁으로 "새 버전으로 업데이트 하세요 (vX.Y.Z)"를 안내한다. 최신 버전이면 강조 없이 title만 "최신 버전입니다"로 안내한다.

버전 표시를 클릭하면 확인(네이티브 다이얼로그) 후 GitHub Release 목록 페이지를 시스템 기본 브라우저로 연다. 이 앱은 자동 다운로드/설치를 하지 않는다 — 알림과 링크 제공까지만이며, 실제 업데이트(다운로드·설치)는 사용자가 직접 한다.

인터넷 연결이 없거나 GitHub API 호출이 실패해도 핵심 기능(Repository/Commit/Export)에는 영향을 주지 않는다 — REQ-010(인터넷 연결 없이 동작 가능)과 충돌하지 않도록, 이 확인은 실패해도 조용히 무시하고 이전에 알던 상태를 유지한다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## REQ-018 저장소/브랜치 요약 라벨 표시

**정정(2026-08-12, RISK_ISSUES.md 결정 이력 #38)**: 원래는 별도 행(TitleBar)으로 설계했으나, 최종적으로 RepositoryPanel의 경로 텍스트 왼쪽에 합쳐 세로 공간을 아끼는 쪽으로 확정했다. 아래 요구사항은 최종 형태 기준이다.

RepositoryPanel은 로컬 클론 폴더명이 아니라 git remote origin URL에서 유도한 "진짜" 프로젝트 이름을 우선 표시해야 한다 — 사용자가 로컬 클론 폴더명을 임의로 바꿀 수 있어(예: `deep-backend`를 `shallow-backend`로 리네임해서 clone) 폴더명만으로는 실제 프로젝트를 오인할 수 있다.

remote(origin)가 없거나 URL 파싱에 실패하면 로컬 폴더명으로 폴백한다. remote 이름과 로컬 폴더명이 다르면 `프로젝트명 (폴더명)` 형식으로 둘 다 보여주되, 폴더명은 부가정보로 취급해 흐린 색으로 구분한다. 둘이 같으면 중복 표시하지 않는다. 이 라벨 오른쪽에 브랜치명(`/ {브랜치}`), 그다음 저장소 경로 텍스트, 그다음 기존 Browse/Reload/버전배지가 한 행에 나란히 놓인다.

"Git Deploy Extractor — " 접두어는 아예 넣지 않는다 — macOS/Windows 앱 창 제목에 이미 같은 이름이 표시되므로 중복이었다.

라벨이 왼쪽에 붙으면서 저장소 경로 텍스트가 밀려 잘릴 수 있으므로, 경로 텍스트는 ellipsis로 자르고 hover 시 `title` 툴팁으로 전체 경로를 보여준다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## REQ-019 배포 대상 파일 제외 패턴

사용자는 "포함된 파일" 목록에서 특정 확장자·경로 패턴(예: `*.png`, `*.yaml`, `*.md`)에 해당하는 파일을 항상 배포 대상에서 제외할 수 있어야 한다.

이 기능은 기존 REQ-013/§7.2의 파일명 검색과 **역할이 다르다** — 검색은 "지금 당장 찾고 싶은 것만 보기"(포함 필터, 매번 새로 입력하고 지워지는 일시적 조건)인 반면, 이 기능은 "이 종류 파일은 앞으로도 계속 안 올린다"(제외 필터, 한 번 등록하면 재사용하는 지속적 조건)이다. 여러 패턴을 동시에 적용할 수 있어야 한다(예: png와 yaml과 md를 한꺼번에 제외).

패턴 문법은 `.gitignore`와 동일한 관례를 따른다 — 슬래시가 없는 패턴(`*.png`)은 경로 어디에 있든 파일명(마지막 조각)에 매치되고, 슬래시가 있는 패턴(`src/test/**`)은 경로 자체에 매치된다. 이 앱이 이미 git 네이티브 도구이고 사용자도 이 문법에 익숙할 가능성이 높다.

등록한 패턴은 앱을 재실행해도 유지되며, 저장소와 무관하게 전역으로 공통 적용된다. 한 번 입력한 패턴은 목록(이력)에 남아 있고, 사용자가 그중 일부만 켜고 끌 수 있다 — 매번 새로 타이핑할 필요 없이 과거에 썼던 패턴을 다시 켜기만 하면 된다. 켜져 있는(활성) 패턴만 실제로 적용된다.

활성 패턴에 매치되는 파일은 "포함된 파일" 목록에서 완전히 숨겨지고, 배포 대상(Export)에서도 제외된다. "누락된 의존성" 목록에는 적용하지 않는다 — 그 목록은 항상 Java 파일만 나오도록 설계되어 있어(REQ-013) 이 패턴이 사실상 적용될 일이 없다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## REQ-020 포함된 파일 / 누락된 의존성 선택 카운터

사용자는 "포함된 파일"과 "누락된 의존성" 각 목록에서 지금 몇 개가 선택(체크)되어 있고 전체가 몇 개인지 한눈에 볼 수 있어야 한다 — 여기서 "선택"은 Export 대상으로 포함된 파일 수를 뜻한다.

각 패널 제목 옆에 카운터를 표시한다. 3회 자기 반성 검토(RISK_ISSUES.md 결정 이력 #42) 끝에 최종 형식을 확정했다: `(선택 N개/전체 N개(필터 전 전체 N개))`.

- **선택 N개** — 상태 Filter·파일명 검색과 **무관한 절대값**. Export될 파일 수를 정확히 반영해야 하므로(REQ-019 제외 패턴에 매치된 파일만 예외적으로 제외 — 그건 실제로 Export되지 않으니까), 화면 필터로 좁혀도 이 숫자는 줄지 않는다. 최초안은 이 값도 화면 기준으로 계산했으나, 검색으로 화면을 좁히면 실제로는 체크돼 있어 Export될 파일이 카운터에서 누락되는 문제(§6.1 케이스 D와 같은 유형의 위험)를 자기 반성 검토에서 발견해 절대값으로 정정했다.
- **전체 N개** — 상태 Filter/파일명 검색/제외 패턴이 다 적용된 뒤 **현재 화면에 표시된 개수**(기존 확정 그대로 유지).
- **(필터 전 전체 N개)** — 참고용 괄호, 모든 필터를 무시한 진짜 전체 개수.

**추가 (누락된 의존성 50개 초과 경고, 2026-08-20, 실사용 중 나온 요구)**: "누락된 의존성" 목록에서 위 "전체 N개"(화면 표시 기준)가 50을 초과하면 그 부분만 붉은 글씨로 강조하고, hover 시 title 툴팁으로 "50개를 초과했습니다"를 안내한다. 50개 판단 기준은 "전체 N개"와 동일한 값을 쓴다 — 검색으로 화면을 좁혀 그 숫자가 50 이하가 되면 경고도 함께 사라진다(의도된 동작). "선택 N개"·"(필터 전 전체 N개)"는 이 경고와 무관하다. "포함된 파일"에는 이 경고를 적용하지 않는다 — 선택 범위에 따라 자연스럽게 늘어나는 목록이라 "너무 많다"는 신호로 보기 어렵지만, "누락된 의존성"이 많다는 건 정적 분석 범위가 예상보다 넓어졌다는 뜻이라 사용자가 눈여겨볼 만한 신호이기 때문이다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## REQ-021 배포 대상 파일 수동 추가

사용자는 선택된 Branch의 HEAD 트리에 존재하는 임의의 파일을, 커밋 diff나 REQ-013 의존성 분석 결과와 무관하게 직접 지정해 배포 대상에 추가할 수 있어야 한다.

REQ-013과 마찬가지로 "선택 Commit → 변경 파일 수집" 경로와 별개로 HEAD 트리를 직접 참조하는 경로다. 다만 REQ-013이 정적 분석으로 후보를 자동 추천하는 것과 달리, 이 기능은 알고리즘 추천 없이 사용자가 파일 경로를 직접 지정한다.

배경: 팀 개발 환경 특성상(개발 장비 → 팀장 → 내부망 메신저로 파일 전달) 이전에 배포됐어야 할 파일이 변경 이력 추적과 무관한 이유(전달 과정에서의 누락 등)로 배포 대상에서 빠지는 사례가 실제로 있었다. GDE는 망분리 환경에서 내부망에 실제로 뭐가 반영돼 있는지 알 수 없어(코드 참조 그래프를 아무리 정교하게 만들어도 이런 종류의 누락은 원리적으로 탐지 불가 — RISK_ISSUES.md 결정 이력 #48 참고) 이런 누락을 자동으로 잡아낼 수 없다. 대신 사용자가 의심되는 파일을 직접 지정해 함께 추출할 수 있게 해, 매번 별도로 전달을 요청하는 번거로움을 줄인다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력 #48, 2026-08-22)

---

## REQ-022 커밋 이력 필터링 조건 추가 (작성자/Merge 커밋 제외)

사용자는 기존 검색(메시지/파일명)·조회 기간·최대 개수 조건에 더해, 다음 두 조건으로 커밋 목록을 추가로 좁힐 수 있어야 한다.

- **작성자(author) 필터**: 커밋 작성자명 부분 일치(대소문자 무관)로 목록을 좁힌다. 여러 팀원이 커밋하는 저장소에서 특정 인원의 변경만 찾을 때 쓴다.
- **Merge 커밋 제외**: 체크박스 하나로 Merge 커밋을 목록에서 제외한다(git `--no-merges`와 동일한 의미). 배포 대상 파일 추출이 목적이라 Merge 커밋 자체는 대개 노이즈다.

두 조건은 기존 검색(메시지/파일명 모드)·조회 기간·최대 개수와 **동시에(AND로) 적용**된다 — REQ-016의 검색 모드처럼 서로 배타적인 관계가 아니라, 각각 독립적으로 목록을 좁히는 방향으로만 결합된다.

기본값은 둘 다 "필터 없음"(작성자 빈 문자열, Merge 포함)이다 — 기존 사용자에게 동작 변화를 주지 않는다.

추가 (커밋 이력 필터링 개선 논의, 2026-09-14)

---

# 7. Deployment Rules

## DR-001 기준 Branch

배포 대상 파일은 기준 Branch의 HEAD를 기준으로 한다.

---

## DR-002 Commit의 역할

Commit은

배포 대상 파일을 식별하기 위한 용도로만 사용한다.

Commit 시점의 파일을 복원하지 않는다.

---

## DR-003 최신 파일 사용

선택된 Commit에서 변경된 파일을 모두 수집한다.

실제 복사되는 파일은

기준 Branch HEAD의 최신 버전이다.

---

## DR-004 동일 파일

동일 파일이 여러 Commit에서 수정되더라도

최종 결과에는 1회만 포함한다.

---

## DR-005 Merge/Rebase

Merge

Squash Merge

Rebase

Cherry-pick

등 Git 전략과 무관하게

변경 파일 집합만 계산한다.

---

## DR-006 공유 파일

ContractClient.java

CommonUtil.java

등

공유 컴포넌트도

선택 Commit에서 수정되었다면

배포 대상에 포함한다.

---

## DR-007 삭제 파일

삭제 파일은 복사하지 않는다.

Delete List에 기록한다.

---

## DR-008 Rename

Rename를 별도로 감지하지 않는다.

Rename은 이전 파일 삭제 + 새 파일 추가로 처리한다.

이전 이름은 DR-007에 따라 Delete List에 포함되고, 새 이름은 REQ-007에 따라 HEAD 최신 버전으로 배포 대상에 포함된다.

두 규칙을 그대로 적용하면 최종적으로 새 파일명만 남으므로, Rename 감지 로직 없이도 최종 파일명 기준 처리가 자연히 성립한다.

어떤 삭제/추가가 실제로는 하나의 Rename인지 판단하는 것은 Preview 화면에서 사용자가 직접 한다.

---

## DR-009 존재하지 않는 파일

HEAD에 존재하지 않는 경우

복사하지 않는다.

Warning으로 표시한다.

---

## DR-010 Mapping Rule

복사 경로는

Mapping Rule을 적용한다.

단, Spring 표준 구조(`.java`, `resources`)는 DR-011·DR-012에 따라 Mapping Rule 대상에서 제외된다.

DR-010은 표준 구조와 다른 배포 경로가 필요한 예외적인 경우에만 적용된다.

---

## DR-011 Java 소스 파일 경로 유지 (Spring 표준 구조)

`.java` 파일은 Mapping Rule 대상에서 제외한다.

`src/` 하위 패키지 경로 구조를 변환 없이 그대로 Deploy Package에 복사한다.

예)

src/main/java/com/example/sell/interfaces/receipt/controller/GuaranteeListController.java

↓

git-deploy-extracted/src/main/java/com/example/sell/interfaces/receipt/controller/GuaranteeListController.java

컴파일은 내부망 GitLab CI 단계에서 수행하므로, 원본 패키지 경로가 그대로 유지되어야 한다.

---

## DR-012 Resources 경로 유지 (Spring 표준 구조)

`src/main/resources` 하위 파일(static, templates 등)은 Mapping Rule 대상에서 제외한다.

원본 경로 구조를 변환 없이 그대로 Deploy Package에 복사한다.

WEB-INF 등 별도 서버 경로로 재배치하지 않는다.

예)

src/main/resources/static/js/guarantee/list.js

↓

git-deploy-extracted/src/main/resources/static/js/guarantee/list.js

src/main/resources/templates/guarantee/list.html

↓

git-deploy-extracted/src/main/resources/templates/guarantee/list.html

---

## DR-013 Export 대상 경로 덮어쓰기 확인

Export 대상 폴더(`<선택한 부모 디렉터리>/git-deploy-extracted`, REQ-012)에 이미 파일이 있으면

기존 내용을 지우기 전에 확인 팝업을 띄운다.

사용자가 취소하면 Export를 중단하고 기존 내용을 그대로 보존한다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.1, 2026-08-07)

---

## DR-014 Java/Spring 의존성 판정 기준 (REQ-013)

의존성 완결성 검사는 Java/Spring **단일 모듈**만 대상으로 한다(멀티모듈 미지원).

기준 패키지(우리 코드 범위)는 `@SpringBootApplication` 애노테이션이 붙은 클래스를 저장소에서 찾아 그 패키지로 정한다. 못 찾으면 폴백 없이 기능을 비활성화한다.

의존성은 두 가지 경로로 판정한다.

- import 문에 의한 텍스트 참조
- Spring DI: 생성자/필드로 주입되는 인터페이스 → 그 인터페이스를 `implements`하면서 `@Component`/`@Repository`/`@Service`/`@Controller`/`@RestController`/`@Configuration` 중 하나가 붙은 클래스

깊이 제한 없이 전이적 폐쇄까지 추적한다(A가 B를 참조하고 B가 C를 참조하면 C까지 포함).

같은 인터페이스를 구현하는 클래스가 여러 개 있으면 정적 분석으로 하나를 확정하지 않고 전부 후보로 제시한다 — 최종 판단은 사용자가 한다.

정확도 원칙: 실제로 필요한데 놓치는 경우(False Negative)를 막는 것이 최우선이며, 불필요한데 후보로 제안되는 경우(False Positive)는 사용자가 걸러내면 되는 참고용으로 허용한다.

이 검사로 찾아낸 파일은 선택된 Commit의 변경 파일이 아니라 **HEAD 기준으로 직접 포함**된다 — REQ-005~007이 정의하는 "선택 Commit → 변경 파일 수집" 경로와는 별개의, HEAD 트리를 직접 훑는 경로다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §7.2, 2026-08-07)

---

## DR-015 커밋 선택 유지/초기화 규칙 (REQ-015)

재조회(검색어/검색 대상/조회 기간/최대 개수 변경, Repository Reload) 시 `selectedHashes`는 기본적으로 유지한다.

다음 두 경우만 예외로 지운다.

- Repository 전환(Browse) — 다른 저장소의 hash로 git 명령을 시도하면 오류/오동작이 발생할 수 있다.
- Branch 전환 — 최종 배포 내용은 항상 현재 선택된 Branch의 HEAD 기준으로 해석되므로, 다른 Branch의 커밋이 섞이면 의도와 다르게 해석될 위험이 있다.

Repository Reload(같은 저장소를 다시 읽는 것)는 위 두 예외에 해당하지 않으므로 선택을 유지한다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md §6.1, 2026-08-07)

---

## DR-016 업데이트 확인 방식 (REQ-017)

**호출 위치**: Renderer의 CSP(`default-src 'self'`)가 외부 호스트로의 `fetch`를 막고 있어, Main process에서 `GET https://api.github.com/repos/neisii/git-deploy-extractor/releases/latest`를 호출하고 IPC로 결과만 Renderer에 전달한다(git/dialog와 동일한 기존 아키텍처).

**버전 비교**: 응답의 `tag_name`(`vX.Y.Z`)과 `app.getVersion()`을 각각 `/^v?\d+\.\d+\.\d+$/`로 검증 후 major.minor.patch 정수 비교로 판정한다(별도 semver 라이브러리 불필요). 형식이 안 맞으면(로컬/원격 어느 쪽이든) 확인 실패로 처리한다 — 크래시하지 않는다. **"다르다"가 아니라 "원격이 로컬보다 엄격히 크다"일 때만 업데이트 있음으로 판정한다** — 로컬 버전이 같거나 앞선 경우(예: 버전을 올렸지만 아직 태그/릴리스 전인 상태)를 업데이트로 오탐하지 않기 위함.

**캐시**: 확인 결과와 확인 시각을 Renderer `localStorage`(`gde:lastUpdateCheck`)에 저장한다. 앱 시작 시 캐시가 24시간을 넘었거나 없으면만 실제 네트워크 호출을 한다 — 비인증 GitHub API 호출은 시간당 60회 제한이 있어, 여러 사용자가 같은 사내망 IP를 공유하는 환경에서 과도한 호출을 피하기 위함이다. 이 캐시는 앱 상태 초기화 시점에 동기적으로 읽어 들인다(마운트 후 비동기로 채우면 배지가 잠깐 평시 상태로 반짝이는 깜빡임이 생긴다).

**알려진 제약**: 시간당 60회 제한은 IP 단위라, 같은 사내망 공인 IP를 공유하는 여러 사용자가 출근 직후처럼 짧은 시간에 몰리면 일부 사용자의 확인이 조용히 실패할 수 있다("실패 처리" 항목대로 안전하게 저하되지만, 그 사용자에게는 새 버전이 나와도 배지가 계속 평시로 보일 수 있다). 인증 토큰 내장은 이 정도 편의 기능에 비해 과설계로 판단해 채택하지 않는다.

**트리거**: ① 앱 시작 시(캐시 만료 조건부) ② 버전 표시 클릭 시 — 클릭은 캐시 나이와 무관하게 항상 강제로 재확인한다("긴급 패치를 바로 인지할 수 있어야 한다"는 사용자 요구, 2026-08-12). 단, 이미 확인이 진행 중이거나 확인창이 이미 열려 있는 상태에서 또 클릭하면 중복 호출/중복 확인창을 만들지 않고 무시한다(연속 클릭 가드).

**클릭 시 확인창과 재확인의 관계**: 클릭하면 "GitHub 저장소를 여시겠습니까?" 확인창(`dialog.showMessageBox`, DR-013의 덮어쓰기 확인창과 동일한 네이티브 패턴)이 **재확인 완료를 기다리지 않고 즉시** 뜬다. 재확인은 별도로 백그라운드에서 진행되며, 완료되는 대로(확인창의 사용자 응답과 무관하게) 배지 색/툴팁을 갱신한다 — 확인창 문구가 버전 정보를 담고 있지 않아 재확인 결과를 기다릴 이유가 없기 때문이다.

**재확인 진행 표시**: 확인 중에는 기존에 알던 배지 색/툴팁을 그대로 둔 채, 버전 텍스트 옆에 로딩 스피너만 추가로 표시한다. Main의 네트워크 호출에 5초 타임아웃이 있어 이 스피너는 항상 유한 시간 내 사라진다 — Renderer 쪽에 별도 타임아웃을 둘 필요가 없다.

**실패 처리**: 네트워크 오류/타임아웃/응답 형식 불일치 시 조용히 무시한다 — 배지 색/툴팁은 실패 이전 상태를 그대로 유지하고, 캐시도 갱신하지 않아 다음 트리거 때 다시 시도한다. 사용자에게 에러를 노출하지 않는다.

**클릭 시 여는 URL**: 항상 고정된 `https://github.com/neisii/git-deploy-extractor/releases`(릴리스 목록 인덱스)만 연다 — 특정 릴리스 태그로 딥링크하지 않으므로, GitHub API 응답의 `html_url`을 상태로 보관하지 않는다.

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## DR-017 프로젝트 이름 유도 방식 (REQ-018)

**조회 명령**: `git remote get-url origin`(Main process, 재현 테스트로 exit code/출력 확인 — §0.1). origin이 없으면 exit code 2 — 이 경우와 명령 자체가 실패하는 경우 모두 `null`을 반환하고 절대 throw하지 않는다(순수 부가정보 기능이라 실패가 저장소 전환 흐름을 막으면 안 된다).

**파싱 규칙**: URL에서 끝의 `.git` 접미사와 슬래시를 제거한 뒤 `/`로 쪼개 마지막 조각을 취한다. HTTPS(`.git` 있음/없음)·SSH scp-like(`git@host:group/subgroup/repo.git`, GitLab 서브그룹 포함) 세 가지 형식 모두 이 방식으로 정확히 파싱됨을 재현 테스트로 확인했다. 특정 호스트(github.com 등)를 하드코딩하지 않는다 — 이 프로젝트는 사내 GitLab 같은 내부 git 서버도 대상이다(REQUIREDMENT.md §2).

**빌드 도구 설정 파일(예: Gradle `settings.gradle`의 `rootProject.name`)은 참조하지 않는다** — (a) 이 앱은 Java/Gradle 전용이 아니라 빌드 도구마다 파일이 다 달라 일반화가 안 되고, (b) `settings.gradle`은 선언적 설정이 아니라 실행 가능한 Groovy/Kotlin 스크립트라 정적 파싱으로 신뢰성 있게 값을 뽑을 수 없고, (c) 워킹트리에서 파일을 읽으면 GDE에서 **선택한 Branch**가 아니라 로컬에 실제로 체크아웃된 Branch의 값을 읽게 되는 위험이 있다 — 이 프로젝트가 이미 겪고 정한 "파일 내용은 워킹트리가 아니라 `git show <branch>:<path>`로 읽는다" 원칙(DETAILED_DESIGN.md §3.2/§4.2)과 상충한다.

**조회 시점**: Branch 목록 조회와 동시에(`Promise.all`) — Browse/Reload 완료 직후 한 번.

**표시 규칙**: remote 이름이 로컬 폴더명과 다르면 `프로젝트명 (폴더명)`, 같으면 `프로젝트명`만, remote가 없으면 폴더명만. 폴더명(괄호 안)은 기존 "보조 정보" 색 토큰(`--ev-c-text-2`, 저장소 경로/Export 경로와 동일)으로 표시해 본문(remote 이름)과 구분한다.

**정정 — 배치 및 경로 truncation(2026-08-12, RISK_ISSUES.md 결정 이력 #38)**: 이 라벨은 별도 행(TitleBar)이 아니라 RepositoryPanel의 저장소 경로 텍스트 왼쪽에 같은 행으로 합쳐진다(브랜치명도 라벨에 포함: `{라벨} / {브랜치}`). 라벨이 새로 붙으면서 그 행이 붐빌 수 있어, 저장소 경로 텍스트에 `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`을 추가하고 `title` 속성으로 hover 시 전체 경로를 보여준다(`footer-action-bar__export-path`와 동일한 기존 패턴 재사용).

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## DR-018 제외 패턴 매칭·저장·적용 방식 (REQ-019)

**매칭 문법**: `.gitignore` 관례 — 패턴에 `/`가 없으면 경로의 마지막 조각(파일명)에 대한 매치, `/`가 있으면 경로 전체에 대한 매치. `*`는 **한 세그먼트 안에서만**(슬래시를 넘지 않고) 임의의 문자열과 매치되는 와일드카드로 해석한다(구현 시 정규식으로 변환).

**정정 — 지원 범위를 `*`(단일 세그먼트)로 한정, 2026-08-20**: 애초 `.gitignore` 전체 문법(다중 세그먼트 와일드카드 `**`, 부정 패턴 `!`, 트레일링 슬래시 디렉터리 매치 등)까지 지원할지 논의했으나, 이번 구현 범위는 `*`만으로 한정한다 — 아래 "지원하지 않는 것" 참고. 여러 개의 패턴을 동시에 등록하는 것 자체는 이 제한과 무관하게 원래부터 지원한다(REQ-019 "png와 yaml과 md를 한꺼번에 제외"는 이미 이 범위 안에서 가능 — 등록 패턴 개수·확장자 종류에 제한 없음).

**지원하지 않는 것(백로그, RISK_ISSUES.md 결정 이력 #42)**:
- `**`(다중 세그먼트 와일드카드, 예: `a/**/b`) — 세그먼트 경계를 넘는 와일드카드는 매일 `.gitignore`를 쓰는 개발자에게도 헷갈리는 디테일이라, 지금 당장 필요하지 않다면 지원 범위에 안 넣는 게 사용성에 더 안전하다고 판단
- `!`로 시작하는 부정(재포함) 패턴 — 지원하지 않으므로 사용자가 입력하면 리터럴 문자로 취급되어 매치가 거의 안 될 것이다(에러는 아님)
- 트레일링 슬래시 디렉터리 접두사 매치(예: `test/fixtures/`로 그 아래 전부 제외) — "디렉터리 전체 제외"라는 실제 니즈가 나오면 `**` 전체 지원보다 훨씬 단순한(순수 문자열 `startsWith` 비교) 전용 규칙으로 해결 가능. 지금은 요청되지 않아 범위 밖.

**대소문자**: 항상 구분한다 — 이 프로젝트가 경로 비교 전반에서 이미 확정한 원칙(결정 이력 #8, 대소문자 항상 구분)과 일관성을 맞춘다.

**저장**: `localStorage`에 전역 키로 저장(`columnWidths.ts`/`exportPath.ts`와 동일한 기존 관례 — 저장소별 구분 없음). 저장 형태는 `{ pattern: string; enabled: boolean }[]` — 입력한 패턴은 지우지 않는 한 이력에 남고, `enabled`만 토글해서 켜고 끈다.

**적용 방식(파생 계산, 상태 중복 저장 금지)**: 제외 패턴은 `deployFiles` 배열의 `included` 필드를 직접 고치지 않는다 — 상태 Filter/파일명 검색과 동일하게, `DeployFilesPanel.tsx`가 화면에 표시할 목록(`includedItems`)을 계산할 때 활성 패턴에 매치되는 항목을 걸러내는 파생 필터 하나로 취급한다. Export 시에도 "포함된 파일 중 `included:true`인 것"이라는 기존 조건에 "활성 제외 패턴에 매치되지 않는 것"을 AND로 추가한다.

이렇게 파생 계산으로 처리하는 이유: 만약 매치되는 파일의 `included`를 직접 `false`로 고쳐 저장하면, 사용자가 나중에 그 패턴을 비활성화했을 때 이미 꺼진 `included` 값을 다시 켜야 하는 별도의 동기화 로직이 필요해지고, 이는 정확히 RISK_ISSUES.md 결정 이력 #33(저장된 상태와 화면 표시가 어긋나는 버그)이 발생했던 것과 같은 유형의 함정이다. 파생 계산으로 처리하면 패턴을 껐을 때 그 파일이 자동으로 다시 보이고(원래 `included` 값 그대로), 동기화 로직 자체가 필요 없다.

**적용 대상**: "포함된 파일"에만 적용, "누락된 의존성"에는 적용하지 않는다(REQ-013 설계상 그 목록은 항상 `.java` 파일만 나와 이 패턴이 무의미하다).

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력, 2026-08-12)

---

## DR-019 수동 파일 추가 방식 (REQ-021)

**입력 방식**: 자동완성 텍스트 입력 — 선택된 Branch의 HEAD 트리 전체 파일 목록(REQ-013과 동일한 `listTrackedFiles` 소스)에서 입력값으로 실시간 필터링해 후보를 보여준다. 후보 목록에서 선택한 경로만 추가할 수 있고, 일치하는 파일이 없는 자유 텍스트는 추가할 수 없다 — 오타로 인한 무효 경로 추가를 원천 차단한다.

**배치**: "포함된 파일" 목록에 통합한다(별도 목록/컬럼 신설 안 함). 결과적으로 커밋 diff로 들어온 파일과 데이터상 구분되지 않고 완전히 동일하게 취급된다 — REQ-019 제외 패턴, Server Path 매핑, Export 로직 전부 그대로 적용된다.

**UI**: "포함된 파일" 패널 헤더에 트리거 버튼(`+ 파일 추가`)을 두고, 클릭하면 팝업이 뜬다. 팝업 안에는 검색 입력 + 자동완성 후보 목록 + 이번 Preview 결과에 수동으로 추가한 파일의 칩 이력이 모두 들어간다. 팝업은 "포함된 파일" 목록(첫 파일 행) 위로는 내려오지 않도록 최대 높이를 제한하고, 후보가 많으면 팝업 내부에서만 스크롤한다 — 목록 자체를 가리지 않는다.

**시각적 구분 없음**: 수동 추가한 파일을 목록 안에서 색상 등으로 구분하지 않는다. 커밋 diff로 들어온 파일, REQ-013 의존성 후보로 추가된 파일과 동일하게 렌더링된다 — 세 경로 모두 결과적으로 같은 `deployFiles` 배열에 같은 형태로 들어가는 이상 구분할 실익이 적다고 판단했다(구분이 필요한 시점, 즉 추가 직후 확인은 팝업 안 칩 이력이 대신한다).

**제외 패턴과의 관계**: 수동 추가한 파일도 REQ-019 제외 패턴에 예외 없이 동일하게 적용된다. 등록된 제외 패턴에 걸리는 파일을 수동으로 추가해도, 다른 파일과 마찬가지로 목록에서 숨겨지고 Export에서 제외된다 — 출처와 무관하게 하나의 규칙만 적용하는 쪽이 일관성 있다고 판단했다.

**생명주기**: 별도의 영속 상태를 두지 않는다. REQ-013 의존성 후보 추가와 동일하게 `deployFiles`에 직접 추가되며, `[Preview]`를 다시 실행하면(`deployFiles`가 새 Preview 결과로 전체 교체되면서) 함께 사라진다. 팝업 안 칩 이력도 같은 시점에 함께 비운다 — 파일은 목록에서 빠졌는데 칩만 "추가됨"으로 남아있으면 실제 상태와 어긋나는 표시가 되기 때문이다(결정 이력 #33과 같은 유형의 함정을 피한다).

**Preview 재실행 시 소실에 대한 트레이드오프(의도적 결정)**: 수동 추가한 파일은 커밋/분석 결과와 무관한 사용자의 고정된 의도이므로, 원칙적으로는 REQ-013 의존성 후보(Preview가 재계산되면 그 시점의 `deployFiles`를 기준으로 다시 도출돼야 하는 파생 결과라 재계산 후에는 유효성이 보장되지 않음)와 달리 Preview 재실행 후에도 유지하는 쪽이 더 정확하다. 하지만 그러려면 별도의 영속 상태와 Preview 재계산 후 재보정(reconciliation) 로직이 새로 필요해진다 — 논의 끝에 구현 단순성과 REQ-013과의 패턴 일관성을 우선해, 정확성 이득보다 비용이 크다고 보고 동일한 생명주기(Preview 재실행 시 초기화)로 통일했다(RISK_ISSUES.md 결정 이력 #48).

추가 (실사용 중 나온 요구, RISK_ISSUES.md 결정 이력 #48, 2026-08-22)

---

# 8. UI 요구사항

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Git Deploy Extractor — contract2/main                                                    (읽기 전용 표시)   │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Repository                                                                               [Browse...]        │
│ D:\workspace\contract2                                                                   [Reload]           │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Branch : contract2/main ▼                           Search : [ guarantee                  ] [Search]        │
│ 조회 기간 : [ 2026-07-28 ] ~ [ 2026-08-04 ]          최대 [ 100 ] 개                                          │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Commit List                                                    │ Deployment Preview                       │
│─────────────────────────────────────────────────────────────── │──────────────────────────────────────────│
│ □ a8f2d31  guarantee UI                                        │ Files                : 18                │
│ ☑ b61e2ab  guarantee html                                      │ Added                : 3                 │
│ ☑ 8dd9e91  guarantee backend                                   │ Modified             : 15                │
│ □ 5e0f712  payment bug                                         │ Deleted              : 1                 │
│ □ 66a3112  refactoring                                          │                                          │
│                                                                │                                          │
├────────────────────────────────────────────────────────────────┼──────────────────────────────────────────┤
│ Deploy Files (HEAD Latest Version)              ☑ 전체 선택                              Filter: All ▼     │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ ☑ src/main/resources/templates/guarantee/list.html               → (경로 그대로 유지)                      │
│ ☑ src/main/resources/static/js/guarantee/list.js                 → (경로 그대로 유지)                      │
│ ☑ src/main/java/.../GuaranteeListController.java                 → (경로 그대로 유지)                      │
│ ☑ src/main/java/.../ContractClient.java                          → (경로 그대로 유지)                      │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Delete List                                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ old.js                                                                                                       │
├──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Mapping Profile : [contract2-prod ▼]                                                     [Preview] [Export] │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

# 9. Deploy Package 구조

```
git-deploy-extracted/
├── src/main/
│   ├── java/              ← 원본 패키지 경로 구조 그대로 (DR-011)
│   │   ├── com/example/sell/interfaces/receipt/controller/GuaranteeListController.java
│   │   └── com/example/sell/.../ContractClient.java
│   └── resources/         ← 원본 경로 구조 그대로 (DR-012)
│       ├── static/js/guarantee/list.js
│       └── templates/guarantee/list.html
├── delete-list.txt
├── deploy-files.txt
└── deploy-summary.json
```

---

# 10. 비기능 요구사항

- macOS 지원(**정정, 2026-08-09**: Apple Silicon/arm64 전용, Intel Mac(x64) 미지원 — 패키징 용량 절감을 위해 universal 바이너리를 채택하지 않기로 결정. RISK_ISSUES.md 결정 이력 참고)
- Windows 지원
- Local Git Repository 기반 동작
- Git CLI 사용
- 인터넷 연결 없이 동작 가능
- 수천 개 Commit에서도 원활한 조회
- 프로젝트 설정(Profile) 저장 지원

---

# 11. MVP 범위

## 포함

- Repository 선택
- Branch 선택
- Commit 조회
- Commit 다중 선택
- 변경 파일 추출
- 중복 제거
- HEAD 최신 파일 추출
- Mapping Rule 적용
- 배포 파일 개별 선택
- Deploy Package 생성
- TXT / JSON Export

## 제외

- Git Push
- Git Pull
- Git Merge
- GitLab 연동
- GitHub 연동
- FTP/SFTP
- Jenkins 연동
- 운영 서버 배포

---

# 12. 구현 원칙

1. Commit은 파일을 식별하기 위한 용도로만 사용한다.
2. 실제 복사되는 파일은 항상 기준 Branch HEAD의 최신 버전이다.
3. Merge/Rebase 전략에 의존하지 않는다.
4. Git History를 복원하지 않는다.
5. Deploy Package 생성까지만 책임진다.
6. 망 분리 환경에서 안정적으로 사용할 수 있도록 인터넷 연결 없이 동작해야 한다.
