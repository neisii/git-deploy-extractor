# 새 세션 구현 지시 프롬프트

> 이 파일은 문서가 아니라, 구현을 맡을 새 세션에게 그대로 붙여넣을 시작 메시지다.

---

```
Git Deploy Extractor 구현을 시작합니다. 요구사항/설계 문서는 이미 확정되어
저장소(neisii/git-deploy-extractor)에 커밋/푸시되어 있습니다.

먼저 이 순서로 읽어주세요 (짐작하지 말고 실제로 읽어야 합니다):

1. REQUIREDMENT.md — 요구사항 원문. REQ-001~011, DR-001~012가 전부 확정 사항.
2. RISK_ISSUES.md §4(결정 이력 로그) — 왜 지금 이 설계가 됐는지 시간순 이력.
   여기 결정들은 대부분 처음엔 다르게 시도됐다가 지금 형태로 정착한 것들입니다.
3. ARCHITECTURE.md — 기술 스택(Electron+React+TypeScript+Zustand), 모듈 구조,
   프로세스 경계.
4. DETAILED_DESIGN.md — 특히 §0 구현 원칙을 반드시 지켜주세요:
   - §0.1: git/외부 도구 동작에 대한 주장은 반드시 재현 테스트로 검증한다
     (기억이나 문서 서술만 믿지 않는다)
   - §0.2: REQUIREDMENT.md에 없는 걸 임의로 확장·개선하지 않는다 (과설계 금지)
   이 문서엔 실제 git 명령어, 알고리즘, JSON 스키마가 전부 확정돼 있습니다.
   그대로 구현 대상입니다.
5. UI_UX_SPEC.md — 컴포넌트/상태/인터랙션 명세.
6. PHASE_PLAN.md — 구현 순서(Phase 0~6). PRD.md §4의 P0/P1/P2는 중요도
   분류이지 빌드 순서가 아닙니다 — 순서는 이 문서를 따라주세요.

작업 방식:
- 문서에 없는 것을 발견하면 임의로 판단하지 말고 먼저 물어보세요.
- git 관련 동작을 새롭게 응용할 일이 생기면(플래그 추가 등) 실제 테스트
  저장소로 재현 확인 후 진행하세요.
- PHASE_PLAN.md의 Phase 0부터 순서대로 진행하세요. Phase 1~3은 UI 없이
  CLI/단위 테스트로 검증 가능하게 설계되어 있으니, 그 방식 그대로 검증하고
  다음 Phase로 넘어가세요.

지금부터 Phase 0(프로젝트 스캐폴딩)부터 시작해주세요.
```
